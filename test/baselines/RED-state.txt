# Pre-change RED baseline state
# Captured: 2026-06-04T03:52:46Z
# Branch: ulw/p1-foundation @ d1892ef

## S1: hack/check-no-install-sh.sh
FORBIDDEN reference in supported flow: charts/daytona/README.md matches /\binstall\.sh\b/
    657:curl -sSL https://download.daytona.io/install.sh | sudo bash
FORBIDDEN reference in supported flow: charts/daytona-region/README.md matches /\binstall\.sh\b/
    27:  ├─ Runner VMs (EC2, installed via runner/install.sh)
    47:5. **Runner Installation**: Runner VMs are installed separately via `runner/install.sh` and configured with the same S3 bucket via `AWS_*` env vars — see [Declarative Builder Setup](#declarative-builder-setup-byoc) below
    168:This chart configures the snapshot-manager half. Each runner VM configures the matching half via `AWS_*` env vars set before running `runner/install.sh`. The values **must match** — same bucket, same region, credentials that can read and write.
    218:Set the matching `AWS_*` env vars before running `install.sh`:
    227:curl -sSL https://download.daytona.io/install.sh | sudo -E bash
    265:**Runners (EC2 instance profile):** attach the IAM policy to the instance profile of the runner's launch template, then run `install.sh` with `AWS_ACCESS_KEY_ID` and `AWS_SECRET_ACCESS_KEY` left empty. The AWS SDK on the runner will pick up instance-metadata credentials.
    300:Private-image authentication for snapshots is **handled by Daytona's control plane, not by the runner**. The runner has no `DOCKER_AUTH_CONFIG`, no `~/.docker/config.json` lookup, and no `aws ecr get-login-password` code path of its own — searching this chart and `runner/install.sh` confirms it. Pull credentials are obtained centrally per pull and handed to the runner inline with each `INSPECT_SNAPSHOT_IN_REGISTRY` / pull job.
S1 exit=0

## S2: runner container existence in irsa fixture render
no runner container in DaemonSet spec — S2 RED

## S4: AWS_ACCESS_KEY_ID gating in irsa-mode runner-secrets
AWS_ACCESS_KEY_ID in irsa-mode runner-secrets: '' (expected ABSENT after S4 GREEN; non-empty = RED)

## S6: baseline sizes
   53355 test/baselines/daytona-region-baseline.yaml
  151210 test/baselines/daytona-baseline.yaml
  204565 total
